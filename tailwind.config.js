/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
        "./**/*.{html,js,jsx,ts,tsx}", // Example path to scan all HTML and JS files in project
        // Add other paths like:
        // "./src/**/*.{html,js}",
    ],
    theme: {
        extend: {},
    },
    plugins: [],
}